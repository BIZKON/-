## 2025-05-15 - [Accessible Delete Button]
**Learning:** Icon-only buttons (like x for delete) are confusing for screen readers and hard to tap on mobile.
**Action:** Always replace text characters with proper SVG icons (like `Trash2`) and add `aria-label` with context (e.g., "Delete [Item Name]").

# Palette's Journal

## 2025-05-18 - Accessibility in Dynamic Lists
**Learning:** Icon-only buttons in dynamic lists (like ingredient rows) often lack `aria-label`, making them inaccessible to screen readers.
**Action:** Always include `aria-label` with context (e.g., "Delete [Item Name]") for icon-only buttons.
